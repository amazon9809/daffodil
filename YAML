jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Node.js 20 관련 경고를 무시하고 진행하거나
      # 강제 호환 모드를 설정할 때 사용하는 환경 변수
      ACTIONS_ALLOW_USE_UNSUPPORTED_NODEJS_VERSION: true
    steps:
      - uses: actions/checkout@v4
